#include "catch.hpp"
#include "config/menu_builder.hpp"

using namespace shimmer;
TEST_CASE ( "Building a menu from a config object." )
{
        //config conf;
        //std::shared_ptr<event_system> es = std::make_shared<event_system>();
        //menu_builder builder(es);
        //auto menu = builder.build(conf);
}
